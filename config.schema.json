{
  "pluginAlias": "homebridge-silentgliss-gateway",
  "pluginType": "platform",
  "headerDisplay": "Silent Gliss ethernet gateway",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "SilentGlissGateway",
        "required": true
      },
      "address": {
        "title": "Address",
        "description": "DNS name or IP address of the Silent Gliss ethernet gateway.",
        "type": "string",
        "required": true
      },
      "verboseDebug": {
        "title": "Allow verbose debug",
        "type": "boolean"
      }
    }
  },
  "form": [
    "name",
    "address",
		"verboseDebug"
  ]
}
